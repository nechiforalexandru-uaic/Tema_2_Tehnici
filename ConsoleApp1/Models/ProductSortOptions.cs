namespace ProductCatalogSearch
{
    public enum ProductSortBy
    {
        Id,
        Name,
        Price,
        Category,
        StockQuantity,
        CreatedDate
    }

    public enum SortOrder
    {
        Ascending,
        Descending
    }
}